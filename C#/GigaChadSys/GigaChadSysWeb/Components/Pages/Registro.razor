@page "/registro"
@using GigaChadSysWeb.Components.Layout
@layout AuthLayout
@rendermode InteractiveServer

<div class="auth-page">
    <!-- Decorative corner lines -->
    <div class="auth-corner auth-corner-tl"></div>
    <div class="auth-corner auth-corner-br"></div>

    <div class="auth-container">
        <!-- Logo -->
        <div class="auth-logo">
            <div class="auth-logo-icon">
                <span>🏋️</span>
            </div>
            <h1 class="auth-brand">
                <span class="brand-giga">GYGA</span><span class="brand-chad">CHAD</span>
            </h1>
            <p class="auth-tagline">ENTRENA · SUPÉRATE · VIVE</p>
        </div>

        <!-- Title -->
        <h2 class="auth-title">Crear Cuenta</h2>
        <p class="auth-desc">Únete a la comunidad GigaChad</p>

        <!-- Form Card -->
        <div class="auth-card">
            <div class="auth-form-group">
                <label>NOMBRE COMPLETO</label>
                <div class="auth-input-wrapper">
                    <span class="auth-input-icon">👤</span>
                    <input type="text" @bind="nombre" placeholder="Ej: Juan Perez" />
                </div>
            </div>

            <div class="auth-form-group">
                <label>CORREO ELECTRÓNICO</label>
                <div class="auth-input-wrapper">
                    <span class="auth-input-icon">✉️</span>
                    <input type="email" @bind="correo" placeholder="Ej: juan@email.com" />
                </div>
            </div>

            <div class="auth-form-group">
                <label>CONTRASEÑA</label>
                <div class="auth-input-wrapper">
                    <span class="auth-input-icon">🔒</span>
                    <input type="@(mostrarPass ? "text" : "password")" @bind="password" placeholder="Mínimo 6 caracteres" />
                    <button class="auth-eye-btn" @onclick="() => mostrarPass = !mostrarPass" type="button">
                        @(mostrarPass ? "🙈" : "👁️")
                    </button>
                </div>
            </div>

            <div class="auth-form-group">
                <label>CONFIRMAR CONTRASEÑA</label>
                <div class="auth-input-wrapper">
                    <span class="auth-input-icon">🔒</span>
                    <input type="@(mostrarConfirm ? "text" : "password")" @bind="confirmPassword" placeholder="Repite tu contraseña" />
                    <button class="auth-eye-btn" @onclick="() => mostrarConfirm = !mostrarConfirm" type="button">
                        @(mostrarConfirm ? "🙈" : "👁️")
                    </button>
                </div>
            </div>

            <button class="auth-submit-btn" @onclick="Registrar">Crear Cuenta</button>
        </div>

        <!-- Link -->
        <p class="auth-link-text">
            ¿Ya tienes cuenta?
        </p>
        <a class="auth-link" href="/login">Inicia sesión aquí  ›</a>
    </div>
</div>

@code {
    private string nombre = "";
    private string correo = "";
    private string password = "";
    private string confirmPassword = "";
    private bool mostrarPass = false;
    private bool mostrarConfirm = false;

    private void Registrar()
    {
        // Se conectará con BL después
    }
}